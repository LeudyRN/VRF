{
  "name": "vrf-engineering-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix auth-front",
    "dev": "node scripts/dev.mjs",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix auth-front",
    "build": "npm run build --prefix auth-front"
  }
}
