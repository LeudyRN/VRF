{
  "name": "vrf-engineering-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "echo 'Dependencies already bundled in repository'",
    "dev": "bash -c 'npm run dev --prefix backend & npm run dev --prefix auth-front & wait'",
    "build": "npm run build --prefix auth-front"
  }
}
